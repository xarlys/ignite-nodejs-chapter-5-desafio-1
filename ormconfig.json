{
  "username": "postgres",
  "password": "docker",

  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "db_fin_api",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
